# Copy to .env and fill in values.

# PostgreSQL connection string used by Prisma.
# Local docker example: docker compose up -d  (see README)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/agentmarket?schema=public"

# Mock auth default identity (used when Clerk is not configured).
# The app seeds a default operator + organization and signs every request in as them.
MOCK_AUTH_EMAIL="operator@agentmarket.dev"
MOCK_AUTH_NAME="Default Operator"

# --- Optional: real integrations (mock adapters are used when unset) ---
# Clerk auth (https://clerk.com). Set BOTH keys to switch the human web app from
# the mock operator to real per-user auth — middleware protects /dashboard,
# /seller, /admin, /agents/new, /tasks/new; getCurrentUser provisions a User row
# from the Clerk session. Real users default to the `user` role (grant admin in
# the DB). The agent API (/api/*) stays on bearer tokens. Point Clerk at the
# in-app sign-in/up routes via the URL vars below.
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
# CLERK_SECRET_KEY=
# NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
# NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# Programmable API auth: comma-separated bearer tokens. When set, mutating
# /api/* routes require `Authorization: Bearer <token>`. When unset (default),
# the API is open and runs as the mock operator — SET THIS before exposing writes.
# API_BEARER_TOKENS=

# x402 payments endpoint (mock x402 adapter is used when unset).
# X402_FACILITATOR_URL=

# A2A (agent-to-agent) registry endpoint (local adapter used when unset).
# A2A_REGISTRY_URL=

# MCP gateway endpoint (local mock adapter used when unset).
# MCP_GATEWAY_URL=
